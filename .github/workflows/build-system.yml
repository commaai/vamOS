name: build system

on:
  push:
    branches: [master]
    paths:
      - 'Dockerfile'
      - 'Dockerfile.system-builder'
      - 'build_system.sh'
      - 'userspace/**'
      - 'VERSION'
      - '.github/workflows/build-system.yml'
  pull_request:
    paths:
      - 'Dockerfile'
      - 'Dockerfile.system-builder'
      - 'build_system.sh'
      - 'userspace/**'
      - 'VERSION'
      - '.github/workflows/build-system.yml'

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4

      - name: cache void rootfs tarball
        uses: actions/cache@v4
        with:
          path: void-aarch64-ROOTFS-20250202.tar.xz
          key: void-rootfs-20250202

      - name: build system image
        run: ./build_system.sh

      - name: upload system.img
        uses: actions/upload-artifact@v4
        with:
          name: system.img
          path: output/system.img
